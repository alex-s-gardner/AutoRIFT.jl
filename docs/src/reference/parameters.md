```@meta
CurrentModule = AutoRIFT
```

# Parameters

`params` builds the `Params` a pass reads. Its keywords are the whole configuration surface:
`autorift`'s own keywords are forwarded to it.

```@docs
params
Params
Extent
extent
chip_sizes
chip_measures
measure_at
filter_width
filter_reach
ImageElement
Backend
CPU
MetalGPU
CUDAGPU
isgpu
```
