import{_ as t,o as a,c as s,ao as i}from"./chunks/framework.ImCdirEK.js";const g=JSON.parse('{"title":"Correlating scenes larger than memory","description":"","frontmatter":{},"headers":[],"relativePath":"howto/large-scenes.md","filePath":"howto/large-scenes.md","lastUpdated":null}'),o={name:"howto/large-scenes.md"};function r(n,e,h,l,d,p){return a(),s("div",null,[...e[0]||(e[0]=[i('<h1 id="Correlating-scenes-larger-than-memory" tabindex="-1">Correlating scenes larger than memory <a class="header-anchor" href="#Correlating-scenes-larger-than-memory" aria-label="Permalink to &quot;Correlating scenes larger than memory {#Correlating-scenes-larger-than-memory}&quot;">​</a></h1><p>A pass over a whole scene allocates arrays the size of the scene. <code>process_block_size</code> correlates it a block at a time instead, which bounds peak memory without changing the answer — the result is bit-identical to the untiled call, and only peak memory, the block shape, buffer reuse and the threading shape differ.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">out </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> autorift</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(image1, image2; chip_size </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, process_block_size </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1024</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1024</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span></code></pre></div><p>Worth it whenever peak memory matters, not only when the scene cannot fit.</p><h2 id="Choosing-a-block-size" tabindex="-1">Choosing a block size <a class="header-anchor" href="#Choosing-a-block-size" aria-label="Permalink to &quot;Choosing a block size {#Choosing-a-block-size}&quot;">​</a></h2><p><strong>Peak is set by a block&#39;s area, not by the number of blocks.</strong> Nine arrays sized to the largest read window are held per task, and the task count is capped at <code>min(nblocks, nthreads)</code>, so the footprint is area × threads however finely the scene is cut.</p><p>Measured total process peak on a 17121 × 16961 Landsat overlap at 10 threads, from a memory-mapped input, against an untiled run of the same scene:</p><table tabindex="0"><thead><tr><th style="text-align:right;">block</th><th style="text-align:right;">blocks</th><th style="text-align:right;">peak</th><th style="text-align:right;">runtime</th><th style="text-align:right;">read amplification</th></tr></thead><tbody><tr><td style="text-align:right;">untiled</td><td style="text-align:right;">1</td><td style="text-align:right;">4958 MiB</td><td style="text-align:right;">20.5 s</td><td style="text-align:right;">1.00×</td></tr><tr><td style="text-align:right;">2048 px</td><td style="text-align:right;">81</td><td style="text-align:right;">3116 MiB</td><td style="text-align:right;">24.4 s</td><td style="text-align:right;">1.13×</td></tr><tr><td style="text-align:right;">1024 px</td><td style="text-align:right;">289</td><td style="text-align:right;"><strong>2140 MiB</strong></td><td style="text-align:right;">22.6 s</td><td style="text-align:right;">1.26×</td></tr><tr><td style="text-align:right;">512 px</td><td style="text-align:right;">1089</td><td style="text-align:right;">2248 MiB</td><td style="text-align:right;">22.3 s</td><td style="text-align:right;">1.55×</td></tr><tr><td style="text-align:right;">256 px</td><td style="text-align:right;">4160</td><td style="text-align:right;">2042 MiB</td><td style="text-align:right;">24.0 s</td><td style="text-align:right;">2.21×</td></tr></tbody></table><p>2140, 2248 and 2042 MiB across a 14× range of block counts is the area rule in one line. <code>(1024, 1024)</code> is the general recommendation: near the floor on peak, and past the point where shrinking further buys anything.</p><h2 id="Runtime-is-set-by-the-halo" tabindex="-1">Runtime is set by the halo <a class="header-anchor" href="#Runtime-is-set-by-the-halo" aria-label="Permalink to &quot;Runtime is set by the halo {#Runtime-is-set-by-the-halo}&quot;">​</a></h2><p>Every block reads beyond its own edges, because a chip near the boundary needs the search window around it. That fixed-width margin is the <strong>halo</strong>, and the imagery a run reads grows as</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>((block + 2·halo) / block)²</span></span></code></pre></div><p>which is the fourth column above, at a 69-pixel halo. Below 1024 px the memory curve has flattened while that redundancy keeps climbing — and pushed far enough it stops being merely wasteful: each block read allocates a block-sized temporary, so read amplification is also allocation rate, and a 128-pixel block on this scene becomes GC-bound rather than compute-bound.</p><p><strong>A wide halo therefore needs a proportionally larger block.</strong> A block only a few halos across is mostly overlap, and one smaller than its own halo is rejected rather than silently clipped.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">halo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(grid, p, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(image1))   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># the margin this configuration needs, in pixels, per axis</span></span></code></pre></div><p>The halo need not be square. A configuration with a wide search radius in x and a narrow one in y has an asymmetric halo, and a block shaped to follow it beats a square block of the same area on peak, runtime and thread occupancy at once. Size the two axes separately against <code>halo</code>; that is the floor, not the target.</p><h2 id="Keep-the-block-count-above-the-thread-count" tabindex="-1">Keep the block count above the thread count <a class="header-anchor" href="#Keep-the-block-count-above-the-thread-count" aria-label="Permalink to &quot;Keep the block count above the thread count {#Keep-the-block-count-above-the-thread-count}&quot;">​</a></h2><p>Fewer blocks than threads puts every block in flight at once. On the scene above at 8192 px that held nine 8331² working sets for a peak of <strong>14856 MiB — 3× the untiled run</strong>. Going too large is worse than not blocking at all.</p><p>Blocks are also the unit of threaded work and their cost varies by orders of magnitude, so a small pool waits on its slowest member. Measured occupancy of ten threads:</p><table tabindex="0"><thead><tr><th style="text-align:right;">blocks/thread</th><th style="text-align:right;">10</th><th style="text-align:right;">38</th><th style="text-align:right;">76</th><th style="text-align:right;">148</th><th style="text-align:right;">230</th><th style="text-align:right;">250</th><th style="text-align:right;">581</th></tr></thead><tbody><tr><td style="text-align:right;">threads busy</td><td style="text-align:right;">2.3</td><td style="text-align:right;">4.6</td><td style="text-align:right;">6.6</td><td style="text-align:right;"><strong>8.9</strong></td><td style="text-align:right;"><strong>9.0</strong></td><td style="text-align:right;">7.8</td><td style="text-align:right;">9.0</td></tr></tbody></table><p>Aim for 100–250. The 581 row reaches full occupancy at 21.6× read amplification, which is not a trade worth making.</p><p>A block is a whole number of grid points, so the size is a target that snaps <strong>outward</strong>: at <code>grid_spacing = 32</code> a request of 500 becomes 512. Pass a tuple and only a tuple — a full-width band costs halo on two sides where a square block pays it on four, so the shape is yours to choose rather than something to infer from a scalar.</p><p>A preprocessing filter that estimates from the whole image cannot be reproduced block by block, and is rejected rather than approximated — see <a href="/AutoRIFT.jl/previews/PR26/reference/parameters#AutoRIFT.filter_reach"><code>AutoRIFT.filter_reach</code></a>.</p><h2 id="Reading-a-pair-that-is-still-on-disk" tabindex="-1">Reading a pair that is still on disk <a class="header-anchor" href="#Reading-a-pair-that-is-still-on-disk" aria-label="Permalink to &quot;Reading a pair that is still on disk {#Reading-a-pair-that-is-still-on-disk}&quot;">​</a></h2><p>For input already in memory, including a memory-mapped array, nothing scene-sized is formed at all. For input still on disk, a block&#39;s window is read once per pass — three chip sizes, coarse and fine — which on a 15901 × 13435 GeoTIFF pair is 3.4–4.4× the scene over a run, against 0.66–0.96× for a single pass. No block size removes that.</p><p><code>cache_budget</code> decides whether to read the pair into memory once instead.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">out </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> autorift</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(r1, r2; process_block_size </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1024</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1024</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), cache_budget </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> :auto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p><code>:auto</code>, the default, compares two volumes computable before anything is read: the pair&#39;s own bytes against the sum of every block&#39;s read window, once per pass. It caches when the first is smaller, and is capped at an eighth of free memory — divided by the thread count for <code>threaded = false</code>, the batch shape where each task decides for itself — so a run competes for a share of what is free rather than the last of it.</p><p><strong>Either the whole pair is cached or none of it is.</strong> There is no partial cache to size, because a blocked run sweeps every block once per pass: a chunk&#39;s next use is a whole sweep away, so a cache below the whole working set has evicted it by then. Simulated on the 15901 × 13435 pair at blocks of 1024, read volume is 3.4× the scene at half the working set and 5.7× at a twentieth, falling below 1× only at the whole of it.</p><p>What it costs and buys: caching a 0.398 GiB pair raised peak from <strong>3.6 to 4.2 GiB</strong> at blocks of 512 px, for <strong>15.2 s against 13.5 s</strong> at 10 threads. The gain narrows as the block grows — 14.1 s either way at 2048 px — since a larger block reads less to begin with.</p><ul><li><p>Set a <strong>byte count</strong> on a machine whose free memory is a poor guide to what this process may take: a container with a cgroup limit, or a host shared with work Julia cannot see.</p></li><li><p>Set <strong><code>nothing</code></strong> to hold peak to the blocks alone.</p></li><li><p><strong>Memory-map the input</strong> to keep the raw pair off the heap entirely. A resident input is never copied, and the kernel can reclaim its pages under pressure where an <code>Array</code> cannot.</p></li></ul><p>Input already in memory is never copied whatever this says, and the result does not depend on it.</p><h2 id="What-reading-from-disk-costs" tabindex="-1">What reading from disk costs <a class="header-anchor" href="#What-reading-from-disk-costs" aria-label="Permalink to &quot;What reading from disk costs {#What-reading-from-disk-costs}&quot;">​</a></h2><p>The answer is <strong>bit-identical</strong> to the same pair materialized, at any block size, on all five layers — a windowed read that computed something subtly different would be worse than one that was merely slow.</p><p>Measured on a Landsat 8/9 pair over Jakobshavn — 17121 × 16961 <code>Float32</code>, 4.48 M grid points, 10 threads — reading from the two GeoTIFFs peaks at <strong>5.4 GiB against 9.0 GiB</strong> for the same run from memory, in <strong>41.3 s against 39.4 s</strong>. Both measure the same 915,488 points.</p><p>The gap is smaller than the raw pair because the lazy run holds that pair too. A <code>UInt16</code> or <code>UInt8</code> scene, which is what Landsat level-1 imagery ships as, holds a half or a quarter as much.</p><h2 id="Budget-for-both" tabindex="-1">Budget for both <a class="header-anchor" href="#Budget-for-both" aria-label="Permalink to &quot;Budget for both {#Budget-for-both}&quot;">​</a></h2><p>Peak is the blocks plus, when it is taken, the raw pair. Size the blocks against the halo on each axis, then decide whether the pair&#39;s own bytes are worth the runtime at small blocks.</p><p>The full measured record — three ways to measure peak memory wrong, where the floor comes from, and the per-granule sweeps — is in <a href="/AutoRIFT.jl/previews/PR26/explanation/memory#Memory">Memory</a>.</p>',39)])])}const k=t(o,[["render",r]]);export{g as __pageData,k as default};
