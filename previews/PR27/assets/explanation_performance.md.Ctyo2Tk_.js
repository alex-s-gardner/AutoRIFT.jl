import{_ as e,o as a,c as r,ao as i}from"./chunks/framework.CkrUYRGj.js";const p=JSON.parse('{"title":"Performance","description":"","frontmatter":{},"headers":[],"relativePath":"explanation/performance.md","filePath":"explanation/performance.md","lastUpdated":null}'),s={name:"explanation/performance.md"};function o(n,t,l,d,h,c){return a(),r("div",null,[...t[0]||(t[0]=[i('<h1 id="Performance" tabindex="-1">Performance <a class="header-anchor" href="#Performance" aria-label="Permalink to &quot;Performance {#Performance}&quot;">​</a></h1><p>What a correlation costs, and which of the several available kinds of parallelism to spend.</p><p>Every number here is measured on an Apple M2 Max, 8 threads, Julia 1.12.5, as a minimum over many samples — a shared machine&#39;s single-shot medians swing by 3× under load. <code>benchmark/run.jl</code> reproduces them.</p><h2 id="One-pair" tabindex="-1">One pair <a class="header-anchor" href="#One-pair" aria-label="Permalink to &quot;One pair {#One-pair}&quot;">​</a></h2><p>1024 × 1024, chip 32, radius 25, spacing 32 — a 900-point grid:</p><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:right;">time</th><th style="text-align:right;">allocation</th></tr></thead><tbody><tr><td style="text-align:right;">serial</td><td style="text-align:right;">124.9 ms</td><td style="text-align:right;">—</td></tr><tr><td style="text-align:right;">8 threads</td><td style="text-align:right;"><strong>38.2 ms</strong></td><td style="text-align:right;">32.5 MiB</td></tr></tbody></table><p>Inner loops allocate nothing per point; the 32.5 MiB is buffers sized once per pass. That is asserted in the test suite rather than hoped for, because a per-point allocation is invisible in a single correlation and decides the throughput of a million of them.</p><h2 id="Parallelism:-pair-level-beats-intra-pair" tabindex="-1">Parallelism: pair-level beats intra-pair <a class="header-anchor" href="#Parallelism:-pair-level-beats-intra-pair" aria-label="Permalink to &quot;Parallelism: pair-level beats intra-pair {#Parallelism:-pair-level-beats-intra-pair}&quot;">​</a></h2><p>Two ways to use eight cores. <code>threaded = true</code> splits one pair&#39;s grid points across threads; the alternative gives each thread a whole pair and its own <a href="/AutoRIFT.jl/previews/PR27/reference/correlating#AutoRIFT.Cache"><code>AutoRIFT.Cache</code></a>. 512 × 512, 16 consecutive pairs:</p><table tabindex="0"><thead><tr><th style="text-align:right;">regime</th><th style="text-align:right;">pairs/sec</th><th style="text-align:right;"></th></tr></thead><tbody><tr><td style="text-align:right;">serial</td><td style="text-align:right;">44.7</td><td style="text-align:right;"></td></tr><tr><td style="text-align:right;">intra-pair threading</td><td style="text-align:right;">116.9</td><td style="text-align:right;">2.6× over serial</td></tr><tr><td style="text-align:right;"><strong>pair-level, one cache per worker</strong></td><td style="text-align:right;"><strong>299.4</strong></td><td style="text-align:right;">6.7× over serial, 2.6× over intra-pair</td></tr></tbody></table><p>Pair-level wins by a factor of 2.6, which is why the cache lifecycle is public API rather than an internal detail: <code>init</code>, <code>reinit!</code> and <code>autorift!</code> exist so a worker can hold its buffers and FFT plans across pairs. <a href="/AutoRIFT.jl/previews/PR27/howto/batch#Correlating-many-pairs">Correlating many pairs</a> is the how-to.</p><p>The reason is the working set. One pair&#39;s correlation is thousands of independent small problems, and splitting one of them across threads shares a cache line budget that a whole pair per thread does not. Prefer pair-level whenever there is more than one pair; reach for <code>threaded = true</code> when there is genuinely only one.</p><h2 id="Cold-start,-and-why-it-is-a-separate-number" tabindex="-1">Cold start, and why it is a separate number <a class="header-anchor" href="#Cold-start,-and-why-it-is-a-separate-number" aria-label="Permalink to &quot;Cold start, and why it is a separate number {#Cold-start,-and-why-it-is-a-separate-number}&quot;">​</a></h2><p>A driver that launches a process per image pair pays startup once per pair, so load and first-call compilation can exceed the correlation. Fresh processes:</p><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:right;">first call</th><th style="text-align:right;">wall</th></tr></thead><tbody><tr><td style="text-align:right;">no wisdom, no precompile workload</td><td style="text-align:right;">3.39 s</td><td style="text-align:right;">4.28 s</td></tr><tr><td style="text-align:right;">persisted FFTW wisdom</td><td style="text-align:right;">2.35 s</td><td style="text-align:right;">3.72 s</td></tr><tr><td style="text-align:right;">precompile workload, wisdom cold</td><td style="text-align:right;">1.13 s</td><td style="text-align:right;">2.40 s</td></tr><tr><td style="text-align:right;"><strong>both, steady state</strong></td><td style="text-align:right;"><strong>0.13 s</strong></td><td style="text-align:right;"><strong>1.36 s</strong></td></tr></tbody></table><p><strong>26× off the first call and 3.1× off the wall clock</strong>, at the cost of <code>using AutoRIFT</code> rising from 0.87 to 1.00 s and 5.9 s of precompilation once per install. FFTW wisdom is persisted to a scratch directory and shared across processes, which is what makes the steady-state row reachable at all; <a href="/AutoRIFT.jl/previews/PR27/reference/correlating#AutoRIFT.warm_plans!"><code>AutoRIFT.warm_plans!</code></a> fills it deliberately.</p><p>Load time is measured on every pull request, because a dependency that quietly pulls in a heavy transitive tree is otherwise discovered in production rather than at review.</p><h3 id="On-a-production-sized-grid-the-cold-plan-costs-more-than-the-correlation" tabindex="-1">On a production-sized grid the cold plan costs more than the correlation <a class="header-anchor" href="#On-a-production-sized-grid-the-cold-plan-costs-more-than-the-correlation" aria-label="Permalink to &quot;On a production-sized grid the cold plan costs more than the correlation {#On-a-production-sized-grid-the-cold-plan-costs-more-than-the-correlation}&quot;">​</a></h3><p>Those rows are a 1024² pair reaching three transform sizes. A production grid reaches dozens, and the cost scales with how many are new. Measured on the golden <code>S1A_..._20151120</code> case — an 11.5 M-point grid, untiled at 10 threads, four fresh processes in sequence:</p><table tabindex="0"><thead><tr><th style="text-align:right;">run</th><th style="text-align:right;">wall</th><th style="text-align:right;">CPU</th></tr></thead><tbody><tr><td style="text-align:right;">first</td><td style="text-align:right;"><strong>28.8 s</strong></td><td style="text-align:right;">71.9 s</td></tr><tr><td style="text-align:right;">second, third, fourth</td><td style="text-align:right;">6.5, 6.5, 6.4 s</td><td style="text-align:right;">48.1, 48.9, 48.3 s</td></tr></tbody></table><p><strong>22 seconds of planning against a 6.5 second correlation</strong>, and the CPU column is what identifies it: the extra is 24 CPU-seconds of real work, not a stretched wall clock. The planner, not the correlator.</p><p><strong>Nothing is missing from the package — this is a deployment property.</strong> <code>__init__</code> calls <code>AutoRIFT.load_wisdom!</code> so every process imports what is on disk, and <a href="/AutoRIFT.jl/previews/PR27/reference/correlating#AutoRIFT.warm_plans!"><code>AutoRIFT.warm_plans!</code></a> calls <code>AutoRIFT.save_wisdom!</code> so every pass persists what it measured. The file is keyed by CPU model and FFTW version and lives in a Julia depot scratchspace — 84 KiB here.</p><p>What that means for a batch driver is the part worth stating plainly: <strong>a process-per-pair driver in a fresh container starts with an empty scratchspace, so every pair pays the full planning cost.</strong> The steady-state row above is unreachable without carrying the wisdom file across processes — bake it into the image, or mount the scratch directory. On a fleet this is the difference between 6.5 s and 28.8 s per pair, which no change inside the correlator approaches.</p><h2 id="Against-the-reference-implementation" tabindex="-1">Against the reference implementation <a class="header-anchor" href="#Against-the-reference-implementation" aria-label="Permalink to &quot;Against the reference implementation {#Against-the-reference-implementation}&quot;">​</a></h2><p><code>autoRIFT</code> 2.1.2&#39;s <code>runAutorift()</code> from the conda-forge build, on the same bytes — 1024 × 1024, chip 32, radius 25, spacing 32, a 900-point grid:</p><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:right;">time</th><th style="text-align:right;">measured</th></tr></thead><tbody><tr><td style="text-align:right;">autoRIFT 2.1.2 (C++ core)</td><td style="text-align:right;">456 ms</td><td style="text-align:right;">900/900</td></tr><tr><td style="text-align:right;">AutoRIFT.jl, serial</td><td style="text-align:right;">148 ms</td><td style="text-align:right;">900/900</td></tr><tr><td style="text-align:right;">AutoRIFT.jl, 8 threads</td><td style="text-align:right;"><strong>41 ms</strong></td><td style="text-align:right;">900/900</td></tr></tbody></table><p><strong>3.1× serial and 11.1× threaded</strong>, with displacements agreeing exactly. The threaded comparison is against what the reference actually does rather than against a parallel reference: its production driver sets <code>mpflag = 0</code> unconditionally, so its own multi-threaded path is never used.</p><h2 id="A-trimmed-binary-for-small-instances" tabindex="-1">A trimmed binary for small instances <a class="header-anchor" href="#A-trimmed-binary-for-small-instances" aria-label="Permalink to &quot;A trimmed binary for small instances {#A-trimmed-binary-for-small-instances}&quot;">​</a></h2><p>97% of an ordinary AutoRIFT process&#39;s 408 MiB memory floor is the Julia runtime; the package is about 7 MiB of it. No optimization inside the package reaches the rest, so <code>app/</code> builds a statically compiled executable instead:</p><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:right;">trimmed binary</th><th style="text-align:right;"><code>julia -t1</code>, same work</th></tr></thead><tbody><tr><td style="text-align:right;">peak RSS, 512² pair</td><td style="text-align:right;"><strong>27.2 MiB</strong></td><td style="text-align:right;">424.2 MiB</td></tr><tr><td style="text-align:right;">peak RSS, 2048² pair</td><td style="text-align:right;"><strong>117.8 MiB</strong></td><td style="text-align:right;">542.7 MiB</td></tr><tr><td style="text-align:right;">wall clock, 512², warm wisdom</td><td style="text-align:right;"><strong>0.06 s</strong></td><td style="text-align:right;">1.20 s</td></tr><tr><td style="text-align:right;">binary</td><td style="text-align:right;">3.2 MiB</td><td style="text-align:right;">—</td></tr></tbody></table><p>Every displacement is bit-identical to the library at both sizes, verified by comparing raw output planes rather than by a tolerance. The 20× wall-clock figure is startup rather than correlation, which is exactly the cost a process-per-pair driver pays every time.</p><p><code>correlation</code> is the one exception, and only across a wisdom boundary: two runs with <em>different</em> FFTW wisdom differ by up to 3.0e-7 on about 20% of points, because the planner picks a different algorithm for the same transform size and a different algorithm reassociates the same sum differently. Two library runs disagree the same way. <code>dx</code> and <code>dy</code> are unaffected. Worth knowing before using a byte-comparison as a regression check.</p><p><code>app/README.md</code> has the build recipe. It needs <code>--experimental</code> on Julia 1.12, so it is a recipe rather than a released artifact.</p><h2 id="Where-the-golden-set&#39;s-parallel-headroom-is" tabindex="-1">Where the golden set&#39;s parallel headroom is <a class="header-anchor" href="#Where-the-golden-set&#39;s-parallel-headroom-is" aria-label="Permalink to &quot;Where the golden set&#39;s parallel headroom is {#Where-the-golden-set&#39;s-parallel-headroom-is}&quot;">​</a></h2><p>All 22 golden cases, untiled from the reference&#39;s captured inputs at 10 threads (<code>tools/golden/profile_all.jl</code>). <code>occ</code> is CPU seconds over wall seconds, so it says how much of the machine the run held; <code>serial</code> is the share of sampling intervals at or below two working threads.</p><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:right;">wall</th><th style="text-align:right;">CPU</th><th style="text-align:right;">occupancy of 10</th><th style="text-align:right;">on the table</th></tr></thead><tbody><tr><td style="text-align:right;">whole set</td><td style="text-align:right;"><strong>328.5 s</strong></td><td style="text-align:right;">1913.2 s</td><td style="text-align:right;"><strong>5.82</strong></td><td style="text-align:right;">137.2 s</td></tr></tbody></table><p><strong>The headroom is inside the threaded correlation, not beside it.</strong> Ranked by the time spent at or below two working threads, the top phases are <code>_correlate_surface!</code>, <code>_track_chunk!</code>, <code>_track_claimed!</code> and <code>_track_threaded!</code> — together over half of it — and the hotspots under them are <code>peak_index</code>, <code>fft_execute!</code> and <code>ifft_execute!</code>. That is the correlation itself running with the machine idle beside it, which happens where a pass has too few points to fill ten threads: the coarse levels, whose grids are decimated by 2, 4 and 8. It is an Amdahl floor rather than a load-balance fault, which is consistent with ordering chunks longest-first buying nothing — see below.</p><p>The stages that run <em>between</em> passes — the hole fill, the open-hole scan, the validity intersection, the grid&#39;s sub-pixel phase, the pad — are now a small share, having been threaded one at a time. Reading the ranking as a priority list is the trap the next section is about.</p><p><strong>One entry in that ranking was self-inflicted, which is the cheapest kind to find.</strong> A reflective <code>sizeof</code>-over-<code>fieldnames</code> closure reading the workspace pool&#39;s byte bound held <code>WORKSPACE_LOCK</code> on every workspace return and accounted for <strong>9.8%</strong> of the set&#39;s serial time. Recording the byte count on the workspace instead took the set from 363.6 s and 2023.2 CPU seconds to the row above — <strong>−9.7% wall and −5.4% CPU</strong> — and removed the entry from the table. The CPU saving is larger than the serial share because the closure also ran during parallel intervals, which that ranking does not count.</p><h2 id="Threading-the-whole-grid-stages:-faster-in-isolation,-invisible-end-to-end" tabindex="-1">Threading the whole-grid stages: faster in isolation, invisible end to end <a class="header-anchor" href="#Threading-the-whole-grid-stages:-faster-in-isolation,-invisible-end-to-end" aria-label="Permalink to &quot;Threading the whole-grid stages: faster in isolation, invisible end to end {#Threading-the-whole-grid-stages:-faster-in-isolation,-invisible-end-to-end}&quot;">​</a></h2><p>The stages that run between correlation passes — the grid&#39;s sub-pixel phase, the hole fill, the open-hole scan, the validity intersection, the pad — are now threaded, and each is faster for it. On a 3400² grid and a 1008² level field at 10 threads:</p><table tabindex="0"><thead><tr><th style="text-align:right;">stage</th><th style="text-align:right;">serial</th><th style="text-align:right;">threaded</th></tr></thead><tbody><tr><td style="text-align:right;"><code>_grid_phase</code></td><td style="text-align:right;">23.8 ms</td><td style="text-align:right;"><strong>3.7 ms</strong></td></tr><tr><td style="text-align:right;"><code>_fill_holes!</code></td><td style="text-align:right;">52.9 ms</td><td style="text-align:right;"><strong>32.9 ms</strong></td></tr></tbody></table><p><strong>It does not move a run&#39;s wall clock, and that is worth recording so the measurement is not repeated.</strong> Warmed paired runs, alternating arms so neither carries the other&#39;s planning cost:</p><table tabindex="0"><thead><tr><th style="text-align:right;">case</th><th style="text-align:right;">with threading</th><th style="text-align:right;">without</th><th style="text-align:right;">measured points</th></tr></thead><tbody><tr><td style="text-align:right;"><code>LC08_L1TP_009011</code>, 5.5 M grid</td><td style="text-align:right;">20.2 s, occupancy 9.14</td><td style="text-align:right;">19.7 s, occupancy 9.15</td><td style="text-align:right;">1,708,352 both</td></tr><tr><td style="text-align:right;">NISAR L2 GSLC, thinned</td><td style="text-align:right;">23.0 s, occupancy 6.28</td><td style="text-align:right;">23.1 s, occupancy 6.23</td><td style="text-align:right;">239,574 both</td></tr></tbody></table><p>The reason is arithmetic rather than a failure: these stages run once per level against a correlation that runs once per point, so 20 ms saved four times over is 1–2% of a twenty-second run — inside the spread. <strong>A stage can top the serial-interval ranking and still be a small share of the run</strong>, because that ranking measures only the intervals where the machine was otherwise idle. Read it as &quot;what would have to be parallel to fill those gaps&quot;, not as &quot;where the time is&quot;.</p><p>The changes stand on their own terms — bit-identical, and strictly faster where they are the cost — but a wall-clock claim is not among the things they buy.</p><h2 id="Rejected:-ordering-correlation-chunks-longest-first" tabindex="-1">Rejected: ordering correlation chunks longest-first <a class="header-anchor" href="#Rejected:-ordering-correlation-chunks-longest-first" aria-label="Permalink to &quot;Rejected: ordering correlation chunks longest-first {#Rejected:-ordering-correlation-chunks-longest-first}&quot;">​</a></h2><p>A chunk&#39;s cost spans orders of magnitude on a Geogrid radius field, so handing the expensive ones out first should tighten the makespan from <code>1 + max/total</code> toward <code>1 + 1/n</code>. Implemented, measured, removed.</p><p><code>dev/GATES.md</code> already bounds the prize at <strong>1.16× ideal</strong>, and the measured gain on the paired runs above was zero, against a real cost: scoring the chunks means one pass over every point&#39;s radii per pass, which is <strong>155 ms on an 11.5 M-point grid, or 1.24 s per run over eight passes</strong>. A guaranteed 1.24 s for an unmeasurable share of a 16% ceiling is the wrong trade. Dynamic claiming already absorbs most of the imbalance, which is why the ceiling is 1.16× and not 4×.</p><h2 id="What-not-to-reach-for" tabindex="-1">What not to reach for <a class="header-anchor" href="#What-not-to-reach-for" aria-label="Permalink to &quot;What not to reach for {#What-not-to-reach-for}&quot;">​</a></h2><ul><li><p><strong>A GPU, on an otherwise free machine.</strong> The device is 2.6–3.0× a single CPU core on the correlation pass and 0.35× eight threads on a whole pair. It is also not additive: handing one core&#39;s work to it buys 1.6%, and oversubscribing costs 22%. Its case is one core against it — the process-per-pair shape — and <a href="/AutoRIFT.jl/previews/PR27/howto/gpu#Correlating-on-a-GPU">Correlating on a GPU</a> is specific about the rest.</p></li><li><p><strong>A coarser transform size.</strong> Sizes are already quantized to what FFTW plans well; coarsening costs more than it saves. <a href="/AutoRIFT.jl/previews/PR27/explanation/memory#Memory">Memory</a> has that measurement.</p></li><li><p><strong>A larger block, for speed.</strong> Block size trades read amplification against thread occupancy and peak memory, and the fastest choice is not the largest. <a href="/AutoRIFT.jl/previews/PR27/howto/large-scenes#Correlating-scenes-larger-than-memory">Correlating scenes larger than memory</a> sizes it.</p></li></ul><p><code>benchmark/README.md</code> carries the rest of the record, including changes that were measured and rejected — a global integral image, SAD and SSD similarity measures, windowed-histogram outlier filtering — each with the numbers that ruled it out. A performance claim in a source comment should trace to one of those tables.</p>',52)])])}const m=e(s,[["render",o]]);export{p as __pageData,m as default};
